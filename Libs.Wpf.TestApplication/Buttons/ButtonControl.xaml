<UserControl
    x:Class="Libs.Wpf.TestApplication.Buttons.ButtonControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:buttons="clr-namespace:Libs.Wpf.TestApplication.Buttons"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="Auto" />
        </Grid.ColumnDefinitions>
        <Button
            Grid.Row="0"
            Grid.Column="0"
            Loaded="ButtonOnLoaded"
            Style="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=buttons:ButtonControl}, Path=ButtonStyle}">
            <Button
                Content="Button" />
        </Button>
        <Button Template="{DynamicResource ButtonTemplate1}"/>
        <TextBlock
            Grid.Row="1"
            Grid.Column="0" />
    </Grid>
</UserControl>
