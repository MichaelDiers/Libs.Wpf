<UserControl x:Class="Libs.Wpf.TestApplication.Controls.FolderSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Libs.Wpf.TestApplication.Controls"
             xmlns:controls="clr-namespace:Libs.Wpf.Controls;assembly=Libs.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style
            TargetType="controls:FolderDragAndDropTextBox">
            <Setter
                Property="Width"
                Value="400" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="Auto" />
        </Grid.ColumnDefinitions>
        <GroupBox
            Grid.Row="0"
            Grid.Column="0"
            Header="No Styles/No values set">
            <controls:FolderSelector />
        </GroupBox>
        <GroupBox
            Grid.Row="1"
            Grid.Column="0"
            Header="No Styles/All values set">
            <controls:FolderSelector
                ButtonToolTip="The tool tip of the button"
                ImageSource="pack://application:,,,/Libs.Wpf;component/Assets/material_symbol_error.png"
                TextBoxToolTip="The tool tip of the text box."
                TextBoxWatermark="The watermark of the text box."
                Text="{Binding Folder, Mode=TwoWay}" />
        </GroupBox>
        <GroupBox
            Grid.Row="2"
            Grid.Column="0"
            Header="All Styles/All values set">
            <GroupBox.Resources>
                <ResourceDictionary
                    Source="pack://application:,,,/Libs.Wpf;component/Styles/DefaultStyles.xaml" />
            </GroupBox.Resources>
            <controls:FolderSelector
                ButtonToolTip="The tool tip of the button"
                ImageSource="pack://application:,,,/Libs.Wpf;component/Assets/material_symbol_error.png"
                TextBoxToolTip="The tool tip of the text box."
                TextBoxWatermark="The watermark of the text box."
                Text="{Binding Folder,Mode=TwoWay}" />
        </GroupBox>
    </Grid>
</UserControl>
